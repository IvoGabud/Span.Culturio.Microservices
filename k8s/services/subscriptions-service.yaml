apiVersion: v1
kind: Service
metadata:
  name: subscriptions-service
  namespace: culturio
spec:
  selector:
    app: subscriptions-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
